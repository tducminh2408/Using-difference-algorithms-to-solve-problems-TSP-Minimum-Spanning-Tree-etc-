The DP Bitmask algorithm show exact solutions while significantly reducing time complexity (O(n^2*(2^n)), enabling execution for larger instances up to n = 21. 
However, for n >= 22, the exponential growth in computation time makes it infeasible to run on my laptop. 
In this problem, the DP algorithm also has high memory consumption, which is much higher than that of the Backtracking algorithm.